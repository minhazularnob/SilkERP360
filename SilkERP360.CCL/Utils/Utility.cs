using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Security.Cryptography;

namespace SilkERP360.CCL.Utils
{
    public class Utility
    {
        //public static System.String EncodeToMD5(System.String IP_str_Text)
        //{
        //    Byte[] originalBytes;
        //    Byte[] encodedBytes;
        //    System.Security.Cryptography.MD5 md5;

        //    //Instantiate MD5CryptoServiceProvider, get bytes for original password and compute hash (encoded password)
        //    md5 = new MD5CryptoServiceProvider();
        //    originalBytes = ASCIIEncoding.Default.GetBytes(IP_str_Text);
        //    encodedBytes = md5.ComputeHash(originalBytes);

        //    //Convert encoded bytes back to a 'readable' string
        //    return BitConverter.ToString(encodedBytes);
        //}

        //public static System.String GenerateSecurityToken()
        //{
        //    System.Guid lcl_obj_GUID = System.Guid.NewGuid();
        //    return lcl_obj_GUID.ToString();
        //}

        //public static string GenerateSecurityToken()
        //{
        //    System.Guid lcl_Seed = new Guid();

        //}
    }
}
